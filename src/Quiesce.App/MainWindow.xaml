<ui:FluentWindow x:Class="Quiesce.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Quiesce"
        Width="1060" Height="720"
        MinWidth="820" MinHeight="560"
        WindowBackdropType="Mica"
        ExtendsContentIntoTitleBar="True"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Quiesce" Icon="pack://application:,,,/Assets/logo-dark.png" />

        <!-- Reboot warning. Lives in the shell rather than on a page because it is a fact about the
             machine, not about whatever page happens to be open: a change that has not taken effect yet
             is equally misleading whichever list the user is reading. Stays up until a restart is
             actually observed - dismissing it would leave the machine in a state the app had stopped
             mentioning. -->
        <Border x:Name="RebootBanner"
                Grid.Row="1"
                Margin="12,0,12,8"
                Padding="16,12"
                CornerRadius="6"
                Visibility="Collapsed"
                Background="#33D29922">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="&#x21BB;" FontSize="20" Margin="0,0,12,0"
                           VerticalAlignment="Top" Opacity="0.85" />
                <StackPanel Grid.Column="1">
                    <TextBlock x:Name="RebootHeadline" FontWeight="SemiBold" FontSize="14" />
                    <TextBlock x:Name="RebootDetail" Margin="0,4,0,0" Opacity="0.8"
                               TextWrapping="Wrap" FontSize="12.5" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Simple, fully-controlled navigation: a themed ListBox + ContentControl. The stock
                 NavigationView needs a page-provider service and brings routing machinery this
                 read-only shell doesn't earn yet; revisit if navigation grows features. -->
            <ListBox x:Name="Nav"
                     Grid.Column="0"
                     Margin="8,4,4,8"
                     BorderThickness="0"
                     Background="Transparent"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     SelectionChanged="OnNavSelectionChanged">
                <ListBox.ItemContainerStyle>
                    <!-- Fully retemplated: the stock ListBoxItem selection brush renders as a solid
                         accent block here, which reads as an error state rather than navigation. -->
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="Root"
                                            Margin="0,2"
                                            Padding="14,10"
                                            CornerRadius="6"
                                            Background="Transparent">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="3" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Border x:Name="Pill"
                                                    Grid.Column="0"
                                                    Width="3" Height="16"
                                                    CornerRadius="2"
                                                    HorizontalAlignment="Left"
                                                    Background="Transparent" />
                                            <ContentPresenter Grid.Column="1" Margin="11,0,0,0" VerticalAlignment="Center" />
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Root" Property="Background" Value="#14FFFFFF" />
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Root" Property="Background" Value="#1FFFFFFF" />
                                            <Setter TargetName="Pill" Property="Background"
                                                    Value="{DynamicResource QuiesceAccentBrush}" />
                                            <Setter Property="FontWeight" Value="SemiBold" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>

            <Border Grid.Column="1" Margin="4,4,12,12">
                <ContentControl x:Name="PageHost" />
            </Border>
        </Grid>
    </Grid>
</ui:FluentWindow>
